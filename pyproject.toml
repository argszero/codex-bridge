[project]
name = "codex-bridge"
version = "1.0.0"
description = "Zero-config proxy that bridges Codex CLI to any OpenAI-compatible model — auto-detects model prefix, rewrites config, and translates Responses API to Chat Completions API."
readme = "README.md"
requires-python = ">=3.11"
